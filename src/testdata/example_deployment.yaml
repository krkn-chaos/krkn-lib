apiVersion: v1
kind: Pod
metadata:
  name: krkn-lib-k8s-test
spec:
  containers:
  - image: docker.io/fedora/tools
    command:
      - /bin/sh
      - "-c"
      - "sleep 60m"
    imagePullPolicy: IfNotPresent
    name: krkn-lib-k8s-test
  restartPolicy: Always