apiVersion: batch/v1
kind: Job
metadata:
  name: {{name}}
  namespace: {{namespace}}
spec:
  template:
    spec:
      containers:
      - name: pi
        image: docker.io/fedora/tools
        command: ["sleep","10"]
      restartPolicy: Never
  backoffLimit: 4