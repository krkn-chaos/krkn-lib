apiVersion: batch/v1
kind: Job
metadata:
  name: {{name}}
  namespace: {{namespace}}
spec:
  template:
    spec:
      containers:
      - name: pi
        image: registry.fedoraproject.org/fedora-toolbox:40
        command: ["sleep","10"]
      restartPolicy: Never
  backoffLimit: 4