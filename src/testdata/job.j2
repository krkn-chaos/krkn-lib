apiVersion: batch/v1
kind: Job
metadata:
  name: {{name}}
  namespace: {{namespace}}
spec:
  template:
    spec:
      containers:
      - name: pi
        image: quay.io/krkn-chaos/krkn-lib:fedtools
        command: ["sleep","10"]
      restartPolicy: Never
  backoffLimit: 4